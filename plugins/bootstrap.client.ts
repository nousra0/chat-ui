// Bootstrap 5 JS (optional: for dropdowns, modals). Import only on client.
import 'bootstrap/dist/js/bootstrap.bundle.min.js'

export default defineNuxtPlugin(() => {
  // Bootstrap is loaded via side-effect import above
})
